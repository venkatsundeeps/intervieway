Tailwind is configured in `tailwind.config.cjs`. Add any custom CSS utilities or tokens here if you prefer to organize styles outside the `app` folder. Currently, base styles live in `app/globals.css`.

